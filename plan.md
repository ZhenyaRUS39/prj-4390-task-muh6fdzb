# Plan: prj-4390-task-muh6fdzb

**Project:** `PRJ-3410`  
**Task ID:** `task-muh6fdzb`  
**Repo:** `prj-4390-task-muh6fdzb`  

## Summary

Декомпозирую задачу "Test after plain text fix" в репо prj-4390-task-muh6fdzb на 3 subtask: проверка окружения, smoke-test (что plain text fix работает, нет regression), финальная верификация + отчёт.

## Subtasks

### 1. Подготовить окружение и клонировать репозиторий

- **ID:** `sub-1`
- **Profile:** `20razrab1`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Склонировать prj-4390-task-muh6fdzb с origin/main, проверить наличие тестов, установить зависимости, зафиксировать baseline (HEAD SHA, статус рабочего дерева).
- **Test plan:** git clone выполнен, git status чистый, HEAD == origin/main SHA зафиксирован в логе, npm/pnpm install завершён без ошибок, список доступных тестовых команд (npm test / pytest / make test) известен.

### 2. Запустить smoke-test plain text fix + регрессию

- **ID:** `sub-2`
- **Profile:** `30razrab2`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Выполнить целевые unit/integration тесты, относящиеся к plain text rendering. Если явных тестов нет — добавить минимальный smoke (input с markdown-like символами *, _, `, [, ], (, ) проверить, что вывод plain). Запустить полный test suite для regression.
- **Test plan:** Целевой smoke-test проходит (символы markdown не рендерятся как форматирование). Полный test suite зелёный, exit code 0. Все failures/warnings задокументированы в комментарии subtask.
- **Dependencies:** `sub-1`

### 3. Финальная верификация и отчёт

- **ID:** `sub-3`
- **Profile:** `20razrab1`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Сверить HEAD с origin/main (должен совпадать, если fix уже в main — иначе зафиксировать SHA fix-коммита), собрать итоговый diff/log тестов, передать тест-репорт Айтишнику/Monica. Не коммитить, не пушить.
- **Test plan:** git log origin/main..HEAD пустой (или явно задокументировано отклонение), test-отчёт содержит: список запущенных тестов, pass/fail counts, время выполнения, вывод релевантных assertion'ов, вердикт PASS/FAIL.
- **Dependencies:** `sub-2`


---
*Generated by Hermes Orchestrator*
